// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Use this data source to filter Xenorchestra storage repositories by certain criteria (name_label, pool_id, host_id, sr_type, tags) for use in other resources.
 */
export function getXoaStorageRepositories(args?: GetXoaStorageRepositoriesArgs, opts?: pulumi.InvokeOptions): Promise<GetXoaStorageRepositoriesResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("xenorchestra:index/getXoaStorageRepositories:getXoaStorageRepositories", {
        "hostId": args.hostId,
        "nameLabel": args.nameLabel,
        "poolId": args.poolId,
        "sortBy": args.sortBy,
        "sortOrder": args.sortOrder,
        "srType": args.srType,
        "tags": args.tags,
    }, opts);
}

/**
 * A collection of arguments for invoking getXoaStorageRepositories.
 */
export interface GetXoaStorageRepositoriesArgs {
    /**
     * The Id of the host the storage repositories exist on. For host-local storage repositories the SR's `container` is the host itself, so this filters the repositories down to a single host when several hosts in the pool share the same SR name.
     */
    hostId?: string;
    /**
     * The name of the storage repositories to match.
     */
    nameLabel?: string;
    /**
     * The Id of the pool the storage repositories exist on.
     */
    poolId?: string;
    /**
     * The storage repository field to sort the results by (id, name*label, size, physical*usage and usage are supported).
     */
    sortBy?: string;
    /**
     * Valid options are `asc` or `desc` and sort order is applied to `sortBy` argument.
     */
    sortOrder?: string;
    /**
     * The type of the storage repositories to match (lvm, udev, iso, user, etc).
     */
    srType?: string;
    /**
     * The tags (labels) applied to the given entity. Not used for filtering if empty.
     */
    tags?: string[];
}

/**
 * A collection of values returned by getXoaStorageRepositories.
 */
export interface GetXoaStorageRepositoriesResult {
    /**
     * The Id of the host the storage repositories exist on. For host-local storage repositories the SR's `container` is the host itself, so this filters the repositories down to a single host when several hosts in the pool share the same SR name.
     */
    readonly hostId?: string;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * The name of the storage repositories to match.
     */
    readonly nameLabel?: string;
    /**
     * The Id of the pool the storage repositories exist on.
     */
    readonly poolId?: string;
    /**
     * The storage repository field to sort the results by (id, name*label, size, physical*usage and usage are supported).
     */
    readonly sortBy?: string;
    /**
     * Valid options are `asc` or `desc` and sort order is applied to `sortBy` argument.
     */
    readonly sortOrder?: string;
    /**
     * The type of the storage repositories to match (lvm, udev, iso, user, etc).
     */
    readonly srType?: string;
    /**
     * The resulting storage repositories after applying the argument filtering. `size`, `physicalUsage` and `usage` are in bytes.
     */
    readonly srs: outputs.GetXoaStorageRepositoriesSr[];
    /**
     * The tags (labels) applied to the given entity. Not used for filtering if empty.
     */
    readonly tags?: string[];
}
/**
 * Use this data source to filter Xenorchestra storage repositories by certain criteria (name_label, pool_id, host_id, sr_type, tags) for use in other resources.
 */
export function getXoaStorageRepositoriesOutput(args?: GetXoaStorageRepositoriesOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetXoaStorageRepositoriesResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("xenorchestra:index/getXoaStorageRepositories:getXoaStorageRepositories", {
        "hostId": args.hostId,
        "nameLabel": args.nameLabel,
        "poolId": args.poolId,
        "sortBy": args.sortBy,
        "sortOrder": args.sortOrder,
        "srType": args.srType,
        "tags": args.tags,
    }, opts);
}

/**
 * A collection of arguments for invoking getXoaStorageRepositories.
 */
export interface GetXoaStorageRepositoriesOutputArgs {
    /**
     * The Id of the host the storage repositories exist on. For host-local storage repositories the SR's `container` is the host itself, so this filters the repositories down to a single host when several hosts in the pool share the same SR name.
     */
    hostId?: pulumi.Input<string | undefined>;
    /**
     * The name of the storage repositories to match.
     */
    nameLabel?: pulumi.Input<string | undefined>;
    /**
     * The Id of the pool the storage repositories exist on.
     */
    poolId?: pulumi.Input<string | undefined>;
    /**
     * The storage repository field to sort the results by (id, name*label, size, physical*usage and usage are supported).
     */
    sortBy?: pulumi.Input<string | undefined>;
    /**
     * Valid options are `asc` or `desc` and sort order is applied to `sortBy` argument.
     */
    sortOrder?: pulumi.Input<string | undefined>;
    /**
     * The type of the storage repositories to match (lvm, udev, iso, user, etc).
     */
    srType?: pulumi.Input<string | undefined>;
    /**
     * The tags (labels) applied to the given entity. Not used for filtering if empty.
     */
    tags?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}
